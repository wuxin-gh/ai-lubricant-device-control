<?xml version="1.0" encoding="utf-8"?>
<!-- Accessibility service capabilities. These must match what the vendored
     DeviceControlAccessibilityService actually uses: gestures (tap/swipe),
     window content retrieval (tree parsing), and screenshot capture.
     canTakeScreenshot is required by the screenshot path — without it the
     capture API returns null. -->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged|typeWindowsChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagReportViewIds|flagRetrieveInteractiveWindows|flagInputMethodEditor|flagIncludeNotImportantViews"
    android:canPerformGestures="true"
    android:canRetrieveWindowContent="true"
    android:canTakeScreenshot="true"
    android:description="@string/accessibility_service_description"
    android:isAccessibilityTool="true"
    android:notificationTimeout="100"
    android:packageNames=""
    android:settingsActivity="dev.devicecontrol.app.ui.MainActivity" />
